[build]
builder = "NIXPACKS"

[deploy]
startCommand = "bash -c 'streamlit run app.py --server.port ${PORT:-8080} --server.address 0.0.0.0 --server.headless true --server.enableCORS false --server.enableXsrfProtection false'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
